<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="fr.isen.eldoradeio">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".rooms.ScheduleActivity">
        </activity>
        <activity android:name=".rooms.BookingActivity">
        </activity>
        <activity android:name=".rooms.RoomsActivity">
        </activity>
        <activity android:name=".profile.GroupActivity">
        </activity>
        <activity android:name=".profile.FavoriteActivity">
        </activity>
        <activity android:name=".profile.ProfileActivity">
        </activity>
        <activity android:name=".authentification.HomeActivity">
        </activity>
        <activity android:name=".authentification.RegisterActivity">
        </activity>
        <activity android:name=".authentification.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
        </activity>
    </application>

</manifest>